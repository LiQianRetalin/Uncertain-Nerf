# P04 复核补充说明（不改动原 P04）

日期：2026-09-13。依据方案助手的《GS_RU_P04复核与P04T唯一取证任务书》。原 `reports/p04/`、`P04-final.zip` 和 `COMPLETE_LIMITED` 状态保持原样；本文件不是对旧包重新取证或追认训练权限。

1. Room Gaussian ID `496572` 在 C、D 两条件分别为 `A=0.0429696/1.0429692`、有效相机数 `0/1`、`G0=5.34125e-6/6.36061e-5`、`OAC=G0/G0`。支持门均未通过。11.91 倍是两条件 **G0 回退值**之比，不是 OAC 相对 G0 的新增放大，也不是支持门失效。它同时说明“各条件内 OAC≤2×G0”不能保证跨条件稳定；必须连同绝对差与来源拆分解读。
2. `ratio_epsilon=1e-12` 用于 `u=bM/b` 的分母判定和“有贡献”口径；`b_support_threshold=1e-6` 用于独立相机支持门。这是两个不同阈值。原 P04 不因此重算。
3. 原 P04 的总 alpha 对齐仅检验合成总体，不独立保证逐 Gaussian 的 `b/bM/m` 归因。P04-T 在开训前另设小型逐像素、逐 Gaussian CPU/张量参考及冻结容差，覆盖遮挡重叠、alpha 跳过、提前终止、全接受、全拒绝和污染区域 `bR`；其结果归入新包而非回填旧 P04。
4. P04 八视图终点观察不能外推真实 100 步增密窗，也不能代替首个 step 0—10000 长统计窗。P04-T 是单独授权的新训练身份；若其证据不足，保留原 Parent、P01—P03 和 P04 的有限结论。
